You are editing a NaradaAI voice-agent project in this Replit.

Goal:
Stop using GPT/OpenAI for text-to-speech and instead use ElevenLabs TTS for all agent voice responses, while keeping Narada’s existing agent creation, reasoning, and UI flows.

Do the following steps:

1) Discover current voice/TTS integration
- Search the repo for any existing TTS or audio generation:
  - terms: "text-to-speech", "TTS", "audio/mpeg", "OpenAI", "gpt-4o-mini", "speech"
- Identify:
  - Which server route(s) return audio for voice agents
  - Where in the code an LLM response is turned into audio

2) Add ElevenLabs configuration
- Add a new environment variable to the server config:
  - ELEVENLABS_API_KEY (do NOT hardcode the key)
- Make sure the code reads process.env.ELEVENLABS_API_KEY and fails fast with a clear error if missing.

3) Implement ElevenLabs TTS helper
- Create a new module, e.g. server/elevenlabs.ts (or similar appropriate place).
- In that file, implement a function like:

  async function synthesizeWithElevenLabs(text: string, voiceId: string): Promise<Buffer>

- Use ElevenLabs TTS streaming endpoint:
  POST https://api.elevenlabs.io/v1/text-to-speech/{voice_id}/stream
- Use headers:
  - xi-api-key: ELEVENLABS_API_KEY
  - Content-Type: application/json
  - Accept: audio/mpeg
- Request body should include:
  - text: the text to speak
  - model_id: "eleven_multilingual_v2" (or the default recommended model)
  - voice_settings with some reasonable defaults, e.g. stability ~0.5, similarity_boost ~0.75
- Return the audio as a Buffer.

4) Replace existing TTS calls with ElevenLabs
- In the route(s) that currently generate audio for voice agents:
  - After the LLM generates the final text answer, call synthesizeWithElevenLabs(text, voiceId)
  - Make sure the route:
    - sets Content-Type: audio/mpeg
    - sends the Buffer returned from ElevenLabs
- If there is any OpenAI TTS integration, remove or bypass it so that only ElevenLabs is used for audio, but keep GPT for text reasoning.

5) Wire voiceId to agents
- For now, use a fixed ElevenLabs voiceId constant, e.g. "EXAVITQu4vr4xnSDxMaL".
- Later we will map Narada agents to specific Eleven voices, but for now just pick one working default voice.
- Optionally:
  - Add an optional field on the agent model (e.g. agent.voiceId) and use that if present, otherwise fall back to the default voiceId.

6) Update frontend voice playback if needed
- Find the frontend code that consumes the audio from the voice agent endpoint.
- Make sure it still works when the endpoint now returns audio/mpeg content from ElevenLabs.
- If needed, adjust:
  - fetch/XHR usage
  - blob handling
  - Audio object creation or audio element src assignment
- Verify that clicking the voice/agent button:
  - Sends text to the backend
  - Gets back ElevenLabs audio
  - Plays audio to the user.

7) Add minimal logging and error handling
- Log a clear error if:
  - ELEVENLABS_API_KEY is missing
  - ElevenLabs returns non-200 status
- Return a friendly JSON error to the frontend in failure cases.

8) Final verification
- Run the app.
- Create or use an existing agent.
- Trigger a voice response and confirm:
  - The text is generated by the LLM as before
  - The audio you hear is synthesized by ElevenLabs TTS.
- Ensure no OpenAI TTS calls remain in the codepath.

Implement all of this now and update any TypeScript types as needed.
